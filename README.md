The .csv files to be read into the notebook have been uploaded separately. Before running the notebook on your machine, you will need to adjust those lines in the
notebook to reflect a local directory. 
